[project]
name = "claude-code-context-llemur"
version = "2.1.0"
description = "Enhanced Context-Llemur with MARM memory layer and AI-readable code development for Claude Code"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "context-llemur>=0.2.2",
]

[project.optional-dependencies]
memory = [
    "pyperclip>=1.8.0",
]

[project.scripts]
ctx-notebook = "src.memory.notebook:cli"
ctx-compile = "src.memory.compiler:cli"